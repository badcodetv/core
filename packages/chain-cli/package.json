{
  "name": "@badcode/chain-cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Generic Solana dev-chain commands: toolchain doctor, local validator lifecycle, build, deploy, IDL export, airdrop. Project-agnostic — see chain/README.md's portability contract.",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "bin": {
    "chain": "./src/bin.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
