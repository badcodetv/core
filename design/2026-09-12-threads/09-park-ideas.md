# Thread 09 — Park the ideas we are not doing yet

You are in `~/projects/badcode/badcode`. Read `CLAUDE.md`, `.claude/skills/new-idea/SKILL.md` and
`docs/ideas/README.md` (the idea inbox: minimal prose, one file per idea, a row in the index).

## Goal

Park these, one file each, in the inbox's format, with the "why not now" stated: we are short of time and
resources, so the shortcut for this season is **music and narration both come out of Suno**.

1. **Make the music ourselves in Ableton; take only the newsreader's voice from Suno.** Suno stays the
   voice engine; the bed is ours.
2. **Ableton automation** from Claude Code, the way `packages/premiere-mcp` drives Premiere: a bridge that
   lets a session build, arrange and bounce. Note what exists to copy (the Premiere UXP-panel-to-WSL
   pattern in `docs/premiere/`).
3. **Sound design in Serum via computer use.** Kai saw a video of GPT Astra 6's computer use designing
   sounds in Serum. Desktop Claude (Fable) and Codex desktop are the candidates. Serum is already the
   camping sheet's reference synth sound (`docs/stories/camping/songs/camping.md` §"How the synths are
   worded").
4. **More Premiere automation** — list the gaps the `premiere-automation` skill's own docs admit
   (`docs/premiere/api-notes.md`: no on-screen text, no whole-timeline read) as the candidates.

Each file: the idea in three lines, what it would buy, what it costs, what already exists in the repo to
build on, and the trigger that would un-park it. No development here — the inbox is the stage before
canon; do not run `new-story`.

## House rules

Shared checkout: one commit on `main`, stage only the four files and the index, never push, never switch
branches. Rundown style.

## First action

Write the four files and the index rows, commit, and reply with the four one-line hooks.
