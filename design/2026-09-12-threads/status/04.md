# Thread 04 — Agent Wolf live
session: t04-wolf-live (was agent-wolf-30)
RESUME HERE
DONE: everything that does not need a server. agent-bob/design/2026-09-12-wolf-deployment.md (7 commits, latest 5e34f45) — full box recipe, now citing ops/docs/build-the-box.md by section name. agent-wolf/docs/for-testers.md + docs/testing-feedback.md (4e6f12f). agent-bob/design/2026-09-12-session-state-and-snapshot-retention.md (cf978e3, concern only, parked by Kai). Local rehearsal green and mock-verified from agentd's boot log; DinD->wolf-api reachability proved at runtime. Bob cleared: 02's D2 green, 01 merged, main 4904a96 verified here.
NEXT (the moment an IP exists): run the recipe top to bottom — folder + clone (NO disk, NO Postgres role), secrets to /srv/apps/wolf/.env, wolf project into Bob's projects.json, Caddy block to 127.0.0.1:8120, DNS A record, `/srv/apps/wolf/up.sh`, expect 401 from /api/auth/me, then bootstrap. Then Kai signs in at wolf.badcode.tv, states a hypothesis, and it scores the next morning.
NEEDS FROM KAI: (1) 🔴 DOES BOB GET ITS OWN HOSTNAME? bob.badcode.tv, or Bob served on box.badcode.tv? Wolf is wolf.badcode.tv either way, but the answer IS the BOB_PUBLIC_URL / VITE_BOB_PUBLIC_URL / allowed_origins value in my §7-§8 (one address, four places, checked by nothing) and it must be right before the wolf-web image is BUILT — vite inlines it. (2) Add https://wolf.badcode.tv to the Google OAuth client's Authorized JavaScript origins; cannot be scripted, can be done now. (3) A yes before the first billable `./stack wolf up` / researcher tick. Already answered: allowlist kaiyadavenport@gmail.com, wolf.badcode.tv, port 8120.
BOX EXISTS (01, 15:20): 217.182.193.214, box.badcode.tv resolves, still installing. UBUNTU not Debian, admin user `ubuntu` not `debian` — my recipe is unaffected (paths + docker compose only, no distro or user assumptions). Parked deliberately: not touching the box until 01 says the build is finished and Kai answers the hostname question.

2026-09-12 — DONE: local rehearsal green. `AO_IMAGE_TAG=04e2703 ./stack wolf up mock --skip-image`
exit 0, all 6 steps; mock model confirmed from agentd's boot log (nothing billed); DinD->wolf-api
reachability proved at runtime (401 from 172.17.0.1:8100/mcp inside agent-bob-dind-1).
DONE: agent-bob/design/2026-09-12-wolf-deployment.md, committed 54e56f8 on main (my file only).
KAI ANSWERED: domain = wolf.badcode.tv (port 8120, already reserved); allowlist = just
kaiyadavenport@gmail.com for now, more added later via a scripted restart (Wolf is stateless).
01 RELAY CLOSED: Step 11d registry-backend gap fixed by 01 (3ddce38); their two warnings verified
in code here and folded into my §4/§9 (commit 838ecd1): never set ALWAYS_PULL on the box, and
ociregistry's Remove is a no-op so snapshots accumulate forever — Wolf's daily researcher makes
that grow per tick. Needs an Artifact Registry cleanup policy; 01 holds it for Kai's yes.
PARKED BY KAI: agent-bob/design/2026-09-12-session-state-and-snapshot-retention.md (cf978e3) —
session state is a whole-container commit per session and NOTHING deletes idle-session snapshots
on any backend (snapshot_ttl_days only covers the named image catalogue). Concern only, no design.
02 RELAY CLOSED: stack down, ports 8080/8081 free, verified (ss + docker ps). 02 has them for D2/B7.
I am NOT waiting on the ports — I am waiting on 01 for a box and on 02's verdict that Bob's main is
stable enough to deploy on. 01 also evaluating pgBackRest over LVM thin pools (942a2f9); my §2
flags that `new-app-volume wolf` may vanish, which costs Wolf nothing (it holds no state).
DONE: agent-wolf/docs/for-testers.md + docs/testing-feedback.md (4e6f12f). All my unblocked work
is finished. 02 says YES deploy on today's main (34 Go pkgs incl. live PG, 1711/1711 web, package
verify PASS, web image builds) but NO browser has seen it — they message me when D2 finishes, and
that is the real go-ahead.
BLOCKED: no box (01), D2 result (02). Kai only: register wolf.badcode.tv (and, to click the
local one, http://localhost:8081) as an Authorized JavaScript origin on the Google OAuth client.
