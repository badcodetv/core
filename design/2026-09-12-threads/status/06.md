# Thread 06 — GPOM stills, variety mode
session: badcode-98 (tmux pane t06-gpom-stills)
DONE: Variety-mode spread rule written into badcode-art-direction + make-comic + flow-prompt (ec942fb). Cut 3's megacity opening designed and its 20 prompts written — 5 descent shots x 4 grades, `scenes/still-fine.md`, linked from the ledger (00d1fe3). Stills->animatic->Veo written into hybrid-method.md step 2 + a recipe in premiere/recipes.md, marked designed-not-run (136207f). Nothing generated, no credits spent, nothing pushed.
NEXT: Relaunch channel 1 (`./scripts/browser-channel.sh up 1`), Kai signs in to Flow in that window, then fire the 20-prompt batch for cut 3 and bring back a contact sheet. The flow MCP is already resolved to channel 1 — Kai's /mcp reconnect fixed that and does NOT need repeating unless flow_status reports a channel other than 1.
NEEDS FROM KAI: one Google sign-in to Flow on channel 1 (port 9222) — it was signed out, confirmed four ways (flow_status, empty project list, PROJECT_NOT_FOUND on a known project, logged-out page title). Both browsers are currently down; Kai was opening and closing them, so I did not relaunch.

- Open calls, both cheap, neither blocking: the empty-chair spread for cuts 7-10 (3 candidates or 1 match-edit; I recommend 3), and whether N4 gets a UK high-street variant alongside the megacity one.
- Cuts 7-10 (the thread's original scope) are PAUSED, not dropped — Kai redirected to cut 3 on 2026-09-12. Prompts for 7-10 already exist in `scenes/remaining-07-10-prompts.md`; cut 7 needs no new images.
- 🔑 Browser ruling I'd hold to: KEEP the two-browser split (branded Chrome on the listening channel, Chrome for Testing elsewhere). It is proven working and going branded everywhere is irreversible per profile. Revisit after camping's listening run. Measured: branded 153.0.8010.36 > CfT 151.0.7922.34.
- 🟡 Worth queueing, not mine: the flow MCP caches its channel in memory and has no way to change it — releasing the lock does not help, only a restart does. Lives in `packages/flow-mcp`, nobody's shared hot spot.
