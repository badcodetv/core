# Thread 02 — Agent Bob: onboarding
session: t02-bob-onboarding
NOW: DONE — 21 of 22 tickets ticked, stack e2e green, ports released. A6 needs a live check on the box (thread 01). Nothing else needed.

- **COMPLETE.** A1–A7, B1–B8, C1–C5, D1, D2 all merged into `main` and ticked. **A6 alone stays
  unticked on purpose** — the e2e stack carries the inline project map, not the map FILE, so the
  SIGHUP reload cannot be exercised there. **Thread 01 has accepted the live check as ops step 11g
  (`f5c482b`)** and will report when it passes on the box; I verified its four code claims and put
  them in A6's Notes (`6d9bc22`). The trap worth knowing: one leftover
  `AGENTKIT_PROJECT_MAP=` line in a real `.env` disables the whole reload **silently**, because
  inline wins before the file is read (`googleauth.go:250-253`, `docker-compose.yml:168`).
- **D2 (the stack e2e) found five defects nothing offline could.** Worst: **A2 shipped unable to
  do the one thing it existed for** — its poll latched off on the first check, before the interview
  session exists, so "Finish setting up this project" never appeared. `examples/web` has NO test
  runner at all, and A2's own e2e SKIPS without `--mock-script`. Three gaps lined up. All fixed;
  onboarding now passes end to end.
- Final gates on `main`: 1711/1711 web tests, `verify-package.sh` PASS, Go 34 pkgs (also green
  against live Postgres), docker web image builds, stack e2e `78 passed / 1 flake / 39 by-design
  skips`, guide 18 pages + 3 real screenshots + 9 captured fixtures, zero broken links.
- **Budget set as Kai chose: soft 50,000 / hard 100,000**, in `docs/ops.md`, `docs/guide/money.md`
  and `.env.example`. No placeholders left.
- 🔴 **TWO THINGS FOR KAI, one urgent:**
  1. **Another session PUSHED `main` to the public GitHub repo.** I never pushed (the rules forbid
     it). `origin/main`'s tip is a commit titled **`wip`** with no body (`88ae096`), which also
     swept up this thread's in-flight files (DI37). 14 tickets of my work are now public because
     someone else chose it. My last two commits are NOT pushed.
  2. **Copy review (DI22):** six of the seven "firsts" sentences were authored by an agent, not
     specified. `first-rewrite` reads "A quiet architect is the alarm" — warns rather than explains.
- Cleanup done: stack down --purge (no fixture project, no abandoned schedule), throwaway
  `bob-t02-throwaway-pg` removed, ports 8080/8081 free. Working tree clean.
