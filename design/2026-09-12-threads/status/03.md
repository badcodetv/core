# Thread 03 — Bob's first three projects + Kai's WhatsApp + agent-enc
session: t03-bob-first-projects
RESUME HERE — DONE: all 3 projects designed (agent-bob/design/2026-09-12-first-three-projects.md, branch thread/03-projects, worktree agent-bob-wt-03, HEAD 5745962). agent-enc repo created (f1e1795, local, no remote). Budget 50k/100k applied. Bob's OWN WAHA container `bob-waha` running on 127.0.0.1:3000, volume bob-waha-data, creds in ~/.config/bob/waha.env (600, outside all repos). Session `default` = SCAN_QR_CODE. Platinum NEVER contacted.
RESUME HERE — NEXT (no permission needed once scanned): (1) GET /api/sessions/default/me → confirm it is Kai's number. (2) GET /api/default/chats → find Richard's chat id. (3) GET /api/default/chats/<id>/messages?limit=50 → report count, date range, first line of 3 most recent; store nothing. If NOWEB read fails, rebuild the container with WHATSAPP_DEFAULT_ENGINE=WEBJS (2 min; pairing is in the volume, no re-scan). THEN STOP — sending is still gated.
RESUME HERE — NEEDS FROM KAI: (a) scan the QR: `cat ~/.config/bob/waha.env` for the password, open http://localhost:3000/dashboard, WhatsApp → Settings → Linked devices. If the session has gone STOPPED/FAILED while idle, `docker restart bob-waha` and re-create it. (b) a yes before any send. (c) what Richard's archive IS. (d) which 2 stories get a character.

RULED: ENC is NOT "ship the coin" — it is RICHARD'S ARCHIVE; Richard drives creatively, Kai enables. His filing system is in the WhatsApp thread, so charter label_rules is TRANSCRIPTION and waits on the read. Government = fictional cast + continuity worker. the-reader.md's 10 rules signed off. agent-orange.md's 10-worker fleet = ground truth (architect must NOT see it). NO generic connect-an-app work — Kai ruled it out as a rabbit hole.
🔴 BUDGET for Kai: 100k/day ≈ 2-4 jobs. ENC fits, government at the edge, MARKETING wants 10+/day and will hit the ceiling most days. Don't raise it — cut marketing's cadence, learn the real cost.
FOR 02: (1) connections merge needs T-CONN-FIX — `PUT /agent/workers/architect {"connections":["*"]}` per project chartered before the merge, or every architect silently loses project_prompt_write. (2) connections proxy hardcodes `Authorization: Bearer` (go/connections/proxy.go:220) and Spec has no header field, so WAHA (X-Api-Key) cannot be a bearer connection — needs an optional header field. (3) Stage 1 needs agentd to SEE ~/projects/badcode/agent-enc; it mounts only agentd-data:/data (docker-compose.yml:136-137). Local path IS a valid git_remote (gitprojection.go:1401-1404) — gap is the mount.
VERIFIED: subagents cannot call AskUserQuestion. Free offline stack: `./stack start mock local`. WAHA invents its own API key + dashboard password at boot if none given, and logs them.
