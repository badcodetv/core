# Thread 01 — OVH box / off Kubernetes
session: agent-bob-98 (pane t01-ovh)
RESUME HERE — DONE 2026-09-12: BOX IS LIVE, ssh ubuntu@box.badcode.tv (217.182.193.214). Ubuntu 26.04.1 'resolute', 32 threads/125GB, RAID1 [UU] x3. build-the-box.md steps 3+4+7 DONE and ticked with observations; huge pages 17641 reserved, THP off, NVMe sched none, all persistent. box/postgres:18 BUILT on the box (PG 18.6, pgBackRest 2.59.1, all 7 extensions). Config STAGED not started. ops repo commits 22d5409/433523d/811846b, NOT pushed.
RESUME HERE — NEXT: blocked at two gates. (a) Tailscale sign-in link is live, then prove `ssh ubuntu@<ts-ip>` from a NEW terminal, then `bootstrap.sh lockdown --i-can-ssh-over-tailscale` (closes public SSH — port 22 is STILL OPEN to the internet right now). (b) step 8 GCS bucket + key (billable) -> then start Postgres, stanza-create, first full backup, steps 9c-9f, 10, 11, 12, 13.
RESUME HERE — NEEDS FROM KAI: (1) open https://login.tailscale.com/a/d4c28f1011504 ; (2) yes to the step-8 gcloud commands (bucket webkit-servers-ovh-backups, SA ovh-backup, 14d soft delete); (3) bob.badcode.tv A record or serve Bob on box.badcode.tv?; (4) healthchecks.io x6; (5) push the ops repo?
MERGED 2026-09-12: thread/01-ovh → main, fast-forward, local main now 4904a96. NOT PUSHED.
  02 was NOT idle (B7 running, stack up), so I resolved on my branch first and checked 02's 3 uncommitted
  files were untouched before and after — they were. Two additive conflicts in `docs/ops.md` step 11d (02's
  A7 + Kai's budget commit vs my one-Postgres rewrite): BOTH SIDES KEPT, incl. 02's step 11f and 4th rule.
  Also corrected my checklist: box takes ANTHROPIC_API_KEY, not the subscription token (02's finding).
DONE: `docs/ops.md` rewritten around **one Postgres 18 + pgBackRest → GCS**. §1.5 = the write-ahead log,
  Step 4 = directories (no thin pool), Step 9 = the PG image + extensions + pgBackRest + timers + per-app role
  guardrails, Step 13 = a restore to a moment BETWEEN backups. Part 3 rewritten. Data loss 1h → ~60s.
DONE: **built and tested the stack, not just researched it.** postgres:18-bookworm reports 18.6-1.pgdg12+2
  (proves the official image IS the PGDG build). pgvector 0.8.6, PostGIS 3.6.4, pg_partman 5.5.0, pg_cron 1.6,
  pg_repack 1.5.3, pgaudit 18.0, pgbackrest 2.59.1 all install. **Correction found by running it: pgaudit NEEDS
  shared_preload_libraries** (research said not) and it fails the whole transaction, taking other extensions down.
DONE: **`go test ./agentdb/` passes on PG18** — ok 232s, 49 migrations, HNSW pgvector index + jsonb label indexes
  created, 151 verbose results with zero skips. **This closes the Bob-on-PG18 question**: recommend it, a fresh
  Bob has no data so install time is the only free moment. Throwaway container + image removed after.
CLOSED 2026-09-12 (was the 🔴): **bookingsystem does NOT use Elasticsearch** — verified myself in
  ~/projects/booking/booking, not taken on trust. Search is Postgres FTS (store/booking.js:145-158 to_tsquery,
  update_search :160-171 to_tsvector); nothing imports @elastic/elasticsearch and nothing reads the three
  settings keys (settings.js:73-75 are their only occurrences repo-wide). package.json:15 + compose + the
  ElasticSearchBox component name are dead. **NoCode + Franchise Cloud can go off on schedule.** pg_search is
  off the critical path. Recorded in the migration doc §4.8, backups doc §3a, ops.md and Kai's order-the-box §6.
HANDED TO KAI 2026-09-12: `docs/ops-order-the-box.md` — one self-contained file: the 4 accounts, the order
  page choices, the reinstall partition table, what to send back, and the 5 open decisions marked not-blocking.
OWNED FOR 02: A6 (add a user with no restart) is now `docs/ops.md` step 11g — a box-only test, since the
  e2e stack uses the INLINE map and inline WINS over the file (googleauth.go:249). Found a hazard doing it:
  a leftover `AGENTKIT_PROJECT_MAP=` in `.env` silently kills the whole reload path with NO error
  (compose:168 passes it through), so 11g checks that first. Will tell 02 when it passes on the box.
AWAITING KAI: (1) time series — pg_partman+pg_cron [rec]? (2) FTS — pg_search AGPL [rec]? (3) Bob on box PG18
  [rec, now proven]? (4) `n8n.badcode.tv` — live workflows? (5) Step 0 accounts + order the box.
NEW 2026-09-12: **binocarlos/ops** — Kai's private "golden repository" for the one server, cloned at
  ~/projects/badcode/ops. I copied all 5 ops docs + bootstrap.sh in, renamed for the new layout
  (docs/build-the-box.md, docs/order-the-box.md, scripts/bootstrap.sh) with every cross-reference
  rewritten, and wrote README.md (the map: spec, what runs where, backup guarantee, the 3 secrets
  never to lose) + CLAUDE.md (where it's hosted, no-secrets rule, stop gates) + .gitignore. Scanned
  for credentials: none. Committed 22d5409 locally, **NOT pushed — Kai's call.** The agent-bob copies
  now carry a "moved, edit there, this will drift" banner (f3c31a7).
