# Thread 05 — Camping cut + listening tools
session: t05-camping (was badcode-d4)
DONE: Claude can hear — listen MCP live, Gemini API transport (browser is a dead end: AI Studio 403s any CDP-driven generation, Trap 4b). suno-diff lens + sunoBoxes = desired-vs-actual diff. First real run on The M3 Lane found the fault. Also unblocked channel 1 for every thread (hung pactl probe). Commits 1c47e37→6b7825a, NOT pushed.
NEXT: file The M3 Lane's real prompt into camping-v6.md (it exists only on Suno today), then write the chosen proposal into the sheet and DRY-RUN it. No credits without a yes.
NEEDS FROM KAI: (1) pick proposal A (trim <1000, force 174 with drum mechanics) or B (keep 117, industrial breakbeat) — note the take Jack likes has NO Reese/wavetable/amen/chant/bagpipes, they were all absent; (2) confirm branded Chrome for ALL channels, not just listening — irreversible per profile, so not acting on a relay; (3) camping-v6.md §2 Q1–Q4 (exclude list, the chant, skip the v6 baseline, tempo); (4) Jack's extra guidance beyond the band name.

- 🟢 UNBLOCKED THE OTHERS: channel 1 was unlaunchable for anyone. Cause = `flow-chrome.sh` probed
  PulseAudio with a bare `pactl info`; WSLg's audio server is wedged (socket answers nothing, pactl
  has no timeout), so launch blocked past `up`'s 20 s window before echoing — hence the empty log.
  All pactl calls now `timeout 3`. Channel 1 up in 3.5 s, LOGGEDIN=yes. Commit 4560492. Told 06 + 08.
- 🟢 listen MCP BUILT + LIVE: studio-dom/studio-client/channel/describe/server, registered in
  .mcp.json. `suno-diff` lens + `sunoBoxes` prompt input. 114 tests, root typecheck green.
  Commits 1c47e37, a3fabe8, 4560492 — none pushed.
- ✅ CORRECTED my own error: branded Chrome 153 > CfT 151, so opening old profiles is an UPGRADE.
  My earlier comment had it backwards (thread 06 measured it). One-way door is the reverse: a
  profile branded 153 touched can never reopen in CfT 151.
- 🔴 WAITING ON KAI (one line): flip `flow-chrome.sh` to branded Chrome for EVERY channel? Relayed
  as his intent via 06 + orchestrator, but it is irreversible per profile so I will not act on a
  relay. Guard currently = listening channel only.
- 🔴 OPEN FAULT: first real describe returned `STUDIO_ERROR: An internal error has occurred.` at
  14:28, two minutes after Kai's hand-typed "hello" succeeded. So the 403 class is not fully dead.
  Variables: Gemini 3.1 Pro vs the default model, and audio vs text. Bisect in progress.
- 🔔 Channel 2 (the ONLY AI-Studio-signed-in channel) is locked by 06's flow MCP, pid 24933 — asked
  them to release it.
- Also owed by Kai: Jack's extra guidance; camping-v6.md §2 Q1–Q4; a yes for 20 Suno credits.
- ✅ 2026-09-12 (late) — `.env` load fixed in listen server (08's catch was REAL: reproduced
  `apiKeySet: false` on the .mcp.json launch path; a passthrough could not have fixed it). Verified
  with the key unset in the shell: apiKeySet true, transport api. Commit 8c2b9ed, 122 tests.
- 🔴 SETTLED: AI Studio refuses CDP-driven generations (Trap 4b) — 10 variables eliminated incl.
  driving Kai's own tab. NOT WSL, not the Chrome build. We do not work around it; API is the route.
- 🔑 FIRST REAL RESULT: The M3 Lane asked 174 BPM D&B, arrived 117 BPM indie breakbeat. Cause =
  Style box 1,186 chars vs the silent 1,000 cap, so the drum mechanics at the tail were never sent.
  Also caught banned melodic vocals at 3:18 and confirmed no lyric line dropped.
  Ledger: docs/listening/log/2026-09-12-141339-the-m3-lane-44f46458.md
- ✅ CORRECTED (08 pushed back and was right): truncation explains The M3 Lane ONLY. Measured all
  33 v5.5 camping style boxes — zero over the cap; `modrb`, the lane that take descends from, is
  899; accepted camping.md §2 is 997. 08 measured its own history: none ever over. So the real
  lesson is narrower — the 1,186-char box was grown BY HAND IN THE BROWSER, and a box that does not
  come from a sheet bypasses the only check we have. NOT retroactive evidence for anything else.
- 🔧 measure-boxes.py gap (told 08): it names boxes by fence POSITION, so camping.md's My Taste
  fence reads as a 1,660-char "style" box and trips the cap check spuriously; and it skips
  ```style-labelled fences, so it found 0 atoms in camping-sheet.md's 33 lanes.
